import { TransformationPipe } from './transformation.pipe';

describe('TransformationPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformationPipe();
    expect(pipe).toBeTruthy();
  });
});
